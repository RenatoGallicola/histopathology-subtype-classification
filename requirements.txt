# Core — enough to run scripts/ablation.py, scripts/tune_alpha.py and the tests
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# Training and inference — needed only to re-run the notebooks
torch>=2.1
torchvision>=0.16
timm>=1.0
huggingface-hub>=0.23
opencv-python>=4.8
Pillow>=10.0
albumentations>=1.4
tqdm>=4.66
